pipeline {
    agent any
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'php:8.1.0-alpine'
                }
            }
            agent { label 'docker_node' }
            steps {
                sh 'gradle --version'
            }
        }
    }
}
