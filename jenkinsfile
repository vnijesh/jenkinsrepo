pipeline {
    agent { label 'dockerserver' }
        docker { image 'php:8.1.0-alpine' }
    }
    stages {
        stage('Test') {
            agent { label 'dockerserver' }
            steps {
                sh 'php --version'
            }
    }
}
