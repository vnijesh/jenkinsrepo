pipeline {
    agent any
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'php:8.1.0-alpine'
                }
            }
            steps {
                sh 'gradle --version'
            }
        }
    }
}
