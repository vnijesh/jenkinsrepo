pipeline {
    agent {
        docker { image 'nginx:latest' }
    }
    stages {
        agent { label 'dockerserver' }
        stage('Test') {
            steps {
                sh 'nginx -v'
            }
        }
    }
}
