pipeline {
    agent { 
        docker { image 'php:8.1.0-alpine' }
    }
    stages {
        stage('build') {
            agent { label 'docker_node' }
            steps {
                sh 'php --version'
        }
     }
  }
}
